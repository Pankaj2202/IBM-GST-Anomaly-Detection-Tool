# Rename to secrets.toml to activate. Keep this file private (never commit it).

# --- optional login for a shared/live URL ---
# app_password = "choose-a-strong-password"

# --- optional AI chat: add ONE provider key to enable free-form conversation ---
# anthropic_api_key = "sk-ant-..."
# model = "claude-sonnet-4-6"
# or:
# openai_api_key = "sk-..."
# model = "gpt-4o-mini"
